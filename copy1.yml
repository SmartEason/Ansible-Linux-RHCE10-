---
# 20260430 11327110 ceUeval8
- hosts: dev
  become: yes
  tasks:
    - name: Create home.html with IP information
      ansible.builtin.copy:
        dest: "{{ dir1 }}/home.html"
        content: "Hometest on {{ ansible_default_ipv4.address }}"

